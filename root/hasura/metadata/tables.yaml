- table:
    schema: accounts
    name: accounts
  array_relationships:
  - name: applications
    using:
      foreign_key_constraint_on:
        column: account_id
        table:
          schema: accounts
          name: applications
- table:
    schema: accounts
    name: applications
  object_relationships:
  - name: account
    using:
      foreign_key_constraint_on: account_id
- table:
    schema: control
    name: applications
